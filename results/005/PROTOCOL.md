# ORG-DIAG-005 fixed prospective protocol

Adopt ASSIGNMENT.md revision 1 without changes. This diagnostic reuses accepted policy-study checkpoints and predictions; it is not fresh training or a replacement policy analysis. The identifier-based subset consists of trial/task IDs 0 and 1 for all 48 histories, eight families and both early arms: 768 pairs, 1,536 actual populations. Target coordinates and predictions remain literal stored values; original future RNG states are not imported into new evaluation.

Before any new evaluations, freeze INPUT_CHECKPOINTS, SUBSET_ROSTER, EVALUATION_SEEDS, INPUT_PROVENANCE, BOOTSTRAP_INDICES, this protocol, source/configuration, tests and job specification in SOURCE_SHA256SUMS. Source input hashes link to the accepted package. Config retains original transfer model fields for provenance; no training is performed and all new streams are explicitly from master2026092108. Each replicate uses seed_for(master,'diagnostic-mutation' or 'diagnostic-survival',regime,history,family,task,replicate); canonical family strings match frozen data. Replicates0..23 are independent; regime tags separate new evaluation streams. Within each replicate, one set of normal pairs and uniforms per generation is shared across OO/OR/RO/RR. Replace all individual absolute angles by the later rule, preserving input traits and order. Continue6..25 with unchanged N32, spectrum.12/.02, normalized H=R30diag4,1R30', beta10 and sequential without-replacement survivor selection.

Store all20 generations of four branch performance outcomes for every replicate, seeds and original frozen predictions/choices. For each checkpoint D_gj=performance_R−performance_O at g6,25. Means, unbiased variance/covariance (R−1 denominator), standard errors and both rule performance levels remain available. B_p,g=(a_p−mean D_g)^2−s_g²/24; H=(mean D25−mean D6)^2−var(D25−D6)/24; X_p=B_p,25−B_p,6−H. Retain negative estimates without flooring or square roots. X also checked against −2[(a−mean D6)(mean D25−mean D6)+(cov(D6,D25)−var(D6))/24]. See DERIVATION.md. Signed errors use prediction minus observed mean contrast. E−M, E−G and G−M differences in B are fixed at both horizons; lower B means smaller expected squared discrepancy, not necessarily better ranking or policy performance.

Conditional ranking CIs: 2000 paired replicate resamples per checkpoint with seed_for(2026092109,'checkpoint-ci',regime,history,family,task,early). Same replicate IDs at6,25. Linear quantiles at(B−1)q using .0125/.9875 produce97.5% marginal intervals, nominal Bonferroni joint95% per checkpoint. Coverage approximate; no simultaneous guarantee over panel. Classify whollypositive, whollynegative, otherwiseunresolved; touchingzero unresolved. Analytic sign ties abs(a)<=1e−12. Count all3×3 horizon categories and both resolved reversal directions. For each policy, count resolved contradiction/agreement/unresolved at both horizons; overlap local contradiction and reversal, local-only, reversal-only and neither. These four overlap categories exhaust checkpoints; 'neither' is not proof of neither mechanism, and unresolved frequency remains explicit. Classification frequencies describe this procedure, not unbiased true error proportions. No operating policy is reselected.

Aggregate each quantity equally over8families×2trials×2earlyarms withinhistory, then24histories per regime. Retain54 family/early-arm scopes including ALL and BOTH, and1296 history/scope summaries. Paired whole-history2000draws, analysis seed2026092109, structuredthenisotropic; same frozen regime arrays reused across every metric/scope. Linear percentiles .025/.975. Report pointwise exploratory intervals, no multiplicity-controlled inference, no separate-interval regime interaction inference; conditional Monte Carlo variability remains in these summaries. Full3×3 counts and policy error/overlap counts available at allscopes.

One bounded HPC job requested norm,1CPU2GiB30minutes, fixed workload1,474,560population-generation updates. No local/login scientific simulation, policy fitting, extra replicates, outcome-selected cases or HPC Codex workers. Focused fixture tests run on allocated HPC before scientific evaluation. Technical same-design corrections permitted with complete provenance; prior packages/handoff/sharedstatus remain unchanged. Source assertions refuse duplicate run. Stream raw trajectories to controlmemory. Independent stored-output audit imports no experiment modules, uses alternate moment formulas and bootstrap count-weights, checks sources/seeds/inputidentity/rawsummaries/classification/erroridentity/intervals/budget and schedulercompletion. Derived reporting and audit do not generate new simulation outcomes.
