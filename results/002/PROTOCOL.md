# ORG-MECH-002 revision 1: fixed prospective execution protocol

This exploratory intervention is motivated by already observed V3 horizon dependence. It reuses all 48 inherited histories and all 3,072 original paired trials, including uniform-direction references. It is not independent training replication. Assignment: ASSIGNMENT.md. V3 source, config and roster are bundled unchanged. No transfer seeds are refreshed: the intervention intentionally reuses every original stream as specified.

Replay V3 generations 1–5 and save both selected populations immediately after generation 5, preserving x/y and ordering. For each early population, continue under original absolute angle phi (O) or phi+pi/2 (R); replace every individual's angle, including parents, without changing traits. Continue through the fixed generation-25 endpoint. Candidate order is parents then offspring; mutation normal pairs and survival uniform arrays are generated once each generation and shared across all branches. Save both Python RNG states after generation 5 and all original seeds. OO/RR are audited against all 153,600 recorded V3 performance values, with 1e-12 discrepancy threshold and bitwise-equivalence counts retained.

Four cells are OO, OR, RO, RR (early state then late rule). Conditional effects, interaction, symmetric rule and state summaries and original OO−RR follow the assignment exactly. All endpoint cell means and contrasts are averaged over eight trials within history before inference. Percentile pointwise 95% bootstrap intervals use 2,000 common resample index sets for all cells/contrasts in each 24-history group. A single random.Random(2026092104) stream processes structured then isotropic regimes, families 0,15,30,45,60,75,90,isotropic in that order. Percentiles use linear interpolation at (B−1)q. No selected subset, multiplicity claim, tested regime interaction or equivalence margin. Every family is retained regardless of sign.

At all 25 generations record performance, centroid, denominator-N covariance, centroid loss and tr(A covariance); verify mean loss equals their sum within 1e-12. In the four-cell record, generations 1–5 are identical within each early-state pair by definition. Full original trajectories are OO and RR; new continuations begin at generation 6. Negative improvement/declines are valid. The components are an accounting identity, not causal mediation. Symmetric rule+state=OO−RR is also a defined identity, not a unique causal attribution or mediation percentage.

Interpret conditional contrasts and their uncertainty before preferring a mechanism. Negative later-rule contrasts in both states support harm from continued O under those states; negative early-state contrasts in both rules support persistent disadvantage of that early O state under those rules. Mixed signs/sizes and unresolved intervals remain visible. The original illustrative structured history 0 / 75° case is reported without promoting it to representative evidence. Local Gaussian expectation remains distinct from finite-pool survival and selected-population performance.

Focused synthetic tests precede scientific outcomes. Freeze all source, full roster and protocol in SOURCE_SHA256SUMS before upload/submission. One norm Slurm job requests one CPU, 2 GiB, 15 minutes. No HPC Codex worker, extra scientific variant, parameter search, new training or fitted predictor. Routine same-design technical fixes, if necessary, must be recorded; no outcome-driven design changes. Prior packages, handoff and shared status remain untouched. Endpoint findings and next discriminating question will be reported regardless of outcome.
