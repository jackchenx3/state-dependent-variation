# ORG-STATE-003 revision 1: fixed eight-cell centroid/configuration/rule intervention

Question, transformation and complete contrast definitions are fixed by ASSIGNMENT.md, bundled with the accepted previous review. The configuration and all source/input hashes are frozen before new counterfactual outcomes in SOURCE_SHA256SUMS. No predictor is fitted. This is an exploratory follow-up prompted by previous outcomes, not independent historical replication.

Use all 3,072 paired rows and generation-5 checkpoints. Cross both centroids with both COMPLETE centered configurations; preserve each configuration donor's ordering, offsets, covariance, higher moments and duplicates within 1e-12. For equal donors use original coordinates directly. Apply absolute phi for O or phi+pi/2 for R to every individual in an independent branch container. Restore recorded Python mutation/survival RNG states after generation 5. Generate each later generation's normals and uniforms once, shared across all eight branches. No new streams, switch times, target subgroups, coefficients, training or horizons. Existing four cells map OOO→OO, OOR→OR, RRO→RO, RRR→RR; compare all 245,760 continuation scores at generations 6–25 to the previous study, retaining bitwise-equality counts and maximum errors. Also check control generation-5 values. A mismatch above 1e-12 stops execution.

Record constructed generation 5 and generations 6–25 only; no invented earlier history for recombined states. Store each constructed initial population, centroid/covariance/performance and centroid/dispersion losses, complete trajectories and endpoints. Source checkpoint file preserves seeds, original individual states and post-generation-5 RNG states. Mean loss equals centroid loss+tr(A covariance) within 1e-12. This accounting is not causal mediation.

Contrast keys: F_ms is later O−R at centroid m/configuration s; M_sr is centroid O−R at configuration s/rule r; S_mr is configuration O−R at centroid m/rule r. IM_s=F_Os−F_Rs; IS_m=F_mO−F_mR; three_factor=IM_O−IM_R=IS_O−IS_R; I_joint=F_OO−F_RR. J_M averages IM_O,IM_R; J_S averages IS_O,IS_R; retain J_M−J_S and verify J_M+J_S=I_joint. All eight cell means and all 21 contrasts are reported. These symmetric summaries are defined causal contrast averages, not a unique attribution or mediation percentage. Interpret conditional signs before averages; no post hoc dominance cutoff.

Average eight paired trials within history/family; 24 histories per regime. Paired whole-history percentile bootstrap uses 2,000 draws and random.Random(2026092105), structured then isotropic, families 0,15,30,45,60,75,90,isotropic; within history IDs ascend 0..23. One set of resample indices shared across all cells/contrasts in a group. Linear interpolation at (B−1)q for percentiles .025,.975. Pointwise exploratory intervals, no multiplicity adjustment, equivalence or separate-interval regime interaction. All negative/inconclusive findings retained.

Small synthetic-fixture tests precede scientific execution. One norm Slurm job requests 1 CPU/2 GiB/15 minutes. Streaming outcomes and bounded aggregate storage keep memory bounded. Freeze protocol, implementation, full roster and input checksums before submission. Any necessary same-design technical fix/retry is documented; no outcome-driven changes. Preserve all preceding packages, handoff and shared historical status. Report limitations and next question without launching an unassigned variant.
